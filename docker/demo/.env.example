# 環境變數配置範例 (Demo)
#
# 使用說明：
# 1. 將此檔案複製為 .env 檔案：cp .env.example .env
# 2. 修改所有 YOUR_* 佔位符為實際的值
# 3. 建議使用強密碼（至少 8 個字符，包含大小寫字母、數字和特殊字符）
# 4. 不要將 .env 檔案提交到版本控制系統中

# ===== PostgreSQL 資料庫配置 =====
# 資料庫名稱
POSTGRES_DB=blog_v2_db
# 使用者名稱
POSTGRES_USER=YOUR_DB_USERNAME
# 密碼（請替換為強密碼）
POSTGRES_PASSWORD=YOUR_DB_PASSWORD

# ===== Redis 快取配置 =====
# Redis 密碼（請替換為強密碼）
REDIS_PASSWORD=YOUR_REDIS_PASSWORD

# ===== Elasticsearch 搜尋引擎配置 =====
# Elasticsearch elastic 用戶密碼（請替換為強密碼）
ELASTIC_PASSWORD=YOUR_ELASTIC_PASSWORD

# ===== Kibana 配置 =====
# Kibana 系統帳號專用密碼（需與 Elasticsearch 中的 kibana_system 用戶密碼一致）
KIBANA_PASSWORD=YOUR_KIBANA_PASSWORD
# Kibana 加密密鑰（至少 32 個字符，用於加密保存的對象、報告等）
# 可使用指令生成：openssl rand -base64 32
KIBANA_ENCRYPTION_KEY=YOUR_KIBANA_ENCRYPTION_KEY_MIN_32_CHARS

# ===== RabbitMQ 訊息佇列配置 =====
# RabbitMQ 管理用戶名稱
RABBITMQ_USER=YOUR_RABBITMQ_USERNAME
# RabbitMQ 管理用戶密碼（請替換為強密碼）
RABBITMQ_PASSWORD=YOUR_RABBITMQ_PASSWORD

