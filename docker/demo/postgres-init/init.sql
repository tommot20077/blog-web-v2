-- PostgreSQL 初始 DDL 腳本 (Demo 環境)
-- 注意：POSTGRES_DB, POSTGRES_USER 和 POSTGRES_PASSWORD 已經透過 docker-compose.yml 中的環境變數自動建立
-- 你可以在下方開始編寫其他的 DDL 語句（如 CREATE TABLE, CREATE INDEX 等）

-- 例如：
-- CREATE TABLE IF NOT EXISTS example (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(100)
-- );
